# ── 核心 RAG 依赖 ────────────────────────────
langchain>=0.1
langchain-community>=0.3
langchain-huggingface>=0.1
langchain-chroma>=0.1
langchain-text-splitters>=0.3
chromadb>=0.5
sentence-transformers>=3.0
huggingface-hub>=0.20
modelscope>=1.15
openai>=1.0

# ── 基础框架（RAG 引擎直接依赖） ──────────────
torch>=2.0
transformers>=4.30
numpy>=1.24

# ── LLM 直接推理（使用预编译 wheel，兼容 Python 3.14+） ──
# llama-cpp-python @ https://abetlen.github.io/llama-cpp-python/whl/cpu/llama-cpp-python/llama_cpp_python-0.3.34-py3-none-win_amd64.whl
# 注：abetlen.github.io 国内不可达，改用 transformers + 原始 safetensors 模型
# 需要时手动安装: pip install llama-cpp-python (需 CMake + VS Build Tools)

# ── 输入源 ───────────────────────────────────
easyocr>=1.7        # OCR（扫描版 PDF）

# ── Web 与工具 ─────────────────────────────
requests>=2.28
duckduckgo_search>=4.0  # 联网搜索（可选）

# ── 分词 ──────────────────────────────────────
jieba>=0.42        # 中文分词（KB 签名关键词提取）
sentencepiece>=0.1  # DeBERTa tokenizer 后端（NLI 模型依赖）
