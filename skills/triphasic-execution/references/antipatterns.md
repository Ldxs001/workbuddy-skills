# Triphasic Execution - 反模式（Antipatterns）

本文档收录使用本技能时的常见错误和注意事项。

---

## 反模式列表

### ❌ AP-01：跳过语义拆分直接规划

**错误做法：**
```
用户："帮我修复这个 bug"
AI：[直接输出任务规划]
```

**正确做法：**
```
用户："帮我修复这个 bug"
AI：[首先输出语义拆分]
   - 主语：用户
   - 目的：修复 bug
   - 诉求：定位并修复代码错误
   - 动机：恢复功能正常
```

---

### ❌ AP-02：跳过审查直接推进

**错误做法：**
```
EXECUTE → [无 REVIEW] → ADVANCE
```

**正确做法：**
```
EXECUTE → REVIEW（✅/❌/⚠️ + 证据）→ ADVANCE
```

---

### ❌ AP-03：同一步骤无限重试

**错误做法：**
```
第 1 次失败 → 重试
第 2 次失败 → 重试
第 3 次失败 → 重试
第 4 次失败 → 重试（❌ 违反 F-08）
```

**正确做法：**
```
第 1 次失败 → 重试
第 2 次失败 → 重试
第 3 次失败 → 换方案（✅ 遵守 F-08）
```

---

### ❌ AP-04：进度文件未创建就执行

**错误做法：**
```
任务规划 → [无 task_progress.py init] → 直接执行
```

**正确做法：**
```
任务规划 → task_progress.py init → 执行（✅ 遵守 F-03）
```

---

### ❌ AP-05：任务完成未调用 complete

**错误做法：**
```
任务执行完 → [无 task_progress.py complete] → 结束
```

**正确做法：**
```
任务执行完 → task_progress.py complete → 输出总结（✅ 遵守 F-09）
```

---

### ❌ AP-06：中英文混排缺少空格

**错误做法：**
```
"运行 settings.py 配置参数"
"使用 problem_logger 记录问题"
```

**正确做法：**
```
"运行 settings.py 配置参数"
"使用 problem_logger 记录问题"
```

---

## 注意事项

1. **语义拆分是强制的** — 收到任务第一个动作必须是语义拆分，不能跳过
2. **规划必须用户确认** — F-02 要求规划输出后需用户确认才能执行
3. **三步循环不可跳过** — Execute→Review→Advance 缺一不可
4. **进度文件必须创建** — F-03 强制要求 init，否则后续 update/complete 会报错
5. **最多 3 次重试** — F-08 强制要求，第 4 次必须换方案

---

## 常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `task_progress.py update` 报错 | 未执行 `init` | 先执行 `task_progress.py init` |
| `complete` 校验失败 | 步骤完成率不足 | 检查哪些步骤未标记为 success |
| 重试次数超限 | 同一方案失败 3 次 | 换方案，禁止第 4 次重试 |
| 进度文件丢失 | 误删除或路径错误 | 重新执行 `init`，或手动创建进度文件 |
